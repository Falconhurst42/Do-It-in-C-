@page "{id:int}"
@model DoItInCpp.Pages.Includes.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Include</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Include.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Include.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Include.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
