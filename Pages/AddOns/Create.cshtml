@page
@model DoItInCpp.Pages.Addons.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create AddOn</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AddOn.Name" class="control-label"></label>
                <input asp-for="AddOn.Name" class="form-control" />
                <span asp-validation-for="AddOn.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AddOn.Tagline" class="control-label"></label>
                <input asp-for="AddOn.Tagline" class="form-control" />
                <span asp-validation-for="AddOn.Tagline" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AddOn.Description" class="control-label"></label>
                <textarea asp-for="AddOn.Description" class="form-control"></textarea>
                <span asp-validation-for="AddOn.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
